import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function WaitingDisplayPage() {
  return <PlaceholderPage title="Waiting Area Display (TV Mode)" description="Large screen: vehicle number and status (Processing/Ready)." />;
}
