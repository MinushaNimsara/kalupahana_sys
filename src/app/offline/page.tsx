import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function OfflinePage() {
  return <PlaceholderPage title="Offline Sync" description="Cache invoices in LocalStorage/IndexedDB. Auto-sync when back online." />;
}
