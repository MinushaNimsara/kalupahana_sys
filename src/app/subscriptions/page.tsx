import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function SubscriptionsPage() {
  return <PlaceholderPage title="Subscription / Monthly Pass" description="Recurring customers. Track usage vs limit (e.g. 4 washes/month)." />;
}
